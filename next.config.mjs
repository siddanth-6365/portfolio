/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com',"github.githubassets.com","images.rawpixel.com"],
      },
};

export default nextConfig;
